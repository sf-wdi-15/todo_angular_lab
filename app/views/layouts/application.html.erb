<!DOCTYPE html>
<html>
<head>
  <title>TodoAngularLab</title>
  <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.3.12/angular.min.js"></script>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body ng-app="ToDoApp">
 <div class="container">
 	<div class="col-sm-12">
	<div ng-controller="ToDoCtrl">
		{{greeting}}
		
		<div>
			<em><strong><span>{{remainingTodos()}} of {{todos.length}} remaining</span></strong></em>
		</div>
		<form ng-submit="addTodo()">
			<div>
				<input type="text" placeholder="Add a To Do" ng-model="newTodo.content">
			</div>
			<div>
				<input type="checkbox" ng-model="newTodo.complete">
			</div>
			<div>
				<button>New ToDo</button>
			</div>
		</form>
		<br>
		<div ng-repeat="todo in todos">
				<input type="checkbox" ng-model="todo.complete">
				<span="{{todo.complete}}">{{todo.content}}</span> 
		</div>
	</div>
	</div>
 </div>
</body>
</html>
